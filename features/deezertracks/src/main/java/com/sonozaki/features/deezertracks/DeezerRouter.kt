package com.sonozaki.features.deezertracks

import androidx.navigation.NavController

/**
 * Interface for navigation to player
 */
interface DeezerRouter {
    fun openPlayerFromDeezer(navController: NavController)
}